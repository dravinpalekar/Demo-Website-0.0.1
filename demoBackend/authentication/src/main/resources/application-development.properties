server.port=8080



jwtCookieName = cookieToken
################################################ Mysql database settings #################################################
spring.datasource.username= root
spring.datasource.password=Admin@123
spring.datasource.url= jdbc:mysql://localhost:3306/website?createDatabaseIfNotExist=true&autoReconnect=true&useSSL=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
################################################ Mysql database settings #################################################



##################################### On Hibernate settings for showing sql query ########################################
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
##################################### On Hibernate settings for showing sql query ########################################

#################### JWT Secret key and Expiration time in millisecond ###############################################
jwtSecretKey= ======================DravinPalekar===========================
#### Means 24 hours ####
jwtExpirationInMillisecond=86400000
#### Means 24 hours ####
#################### JWT Secret key and Expiration time in millisecond ###############################################




########### Colorful console and banner off options ###########
spring.output.ansi.enabled=ALWAYS
#spring.main.banner-mode=off
########### Colorful console and banner off options ###########

#### Lazy initialization means that Spring won't create all beans on startup. Also, Spring will inject no dependencies until that bean is needed ####
spring.main.lazy-initialization=true
#### Lazy initialization means that Spring won't create all beans on startup. Also, Spring will inject no dependencies until that bean is needed ####

################### Max upload size ###################
spring.servlet.multipart.max-file-size=512KB
spring.servlet.multipart.max-request-size=512KB
################### Max upload size ###################


###################### These are the two features for define Custom path of Swagger UI. ############################
########### Custom path for Swagger UI ###########
#springdoc.swagger-ui.path=/api-docs-ui.html

############# Custom path for the raw OpenAPI JSON ###########
#springdoc.api-docs.path=/api-spec
###################### These are the two features for define Custom path of Swagger UI. ############################


##################################### These are the two features for disabling Swagger UI. #############################
#springdoc.api-docs.enabled=false
#springdoc.swagger-ui.enabled=false
##################################### These are the two features for disabling Swagger UI. #############################